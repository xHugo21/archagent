[project]
name = "simple-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "litellm==1.82.6",
    "python-dotenv>=1.2.2",
]
[tool.pyright]
venvPath = "."
venv = ".venv"
