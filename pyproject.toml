[project]
name = "archagent"
version = "0.1.0"
description = "Basic AI Agent"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "litellm==1.82.6",
    "python-dotenv>=1.2.2",
    "textual>=8.2.4",
]
[tool.pyright]
venvPath = "."
venv = ".venv"
